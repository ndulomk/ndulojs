# ndulojs

```bash
npx ndulojs create my-app
cd my-app && bun install && bun dev
```

This package is a thin wrapper around [`@ndulojs/cli`](https://www.npmjs.com/package/@ndulojs/cli).
For the full CLI reference, see [@ndulojs/cli](https://www.npmjs.com/package/@ndulojs/cli).
